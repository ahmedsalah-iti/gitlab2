git init
git branch -m main
git add .
git commit -m "index added"
git remote add origin git@github.com:ahmedsalah-iti/gitlab2.git
git push -u origin main
git branch dev;git branch test;
git branch
git  checkout dev
git branch
touch dev_file.txt
git checkout test
touch test_file.txt
git add .
git commit -m "added dev_file"
git push origin dev
git push origin test
git checkout main
git merge dev -m "dev merged"
git merge test -m "test merged"

- how to remove local branch :
git branch -d dev
git branch -d test
- how to remove the branch remotly
git push origin :dev or git push origin --delete dev
git push origin :test or git push origin --delete test

- how to checkout another branch without commit changes :
by using git stash then we can checkout to another branch because it's saving the changes without commiting .
  

